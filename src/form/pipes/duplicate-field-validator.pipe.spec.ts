import { DuplicateFieldValidatorPipe } from './duplicate-field-validator.pipe';

describe('DuplicateFieldValidatorPipe', () => {
  it('should be defined', () => {
    expect(new DuplicateFieldValidatorPipe()).toBeDefined();
  });
});
