import { DuplicateFormValidatorPipe } from './duplicate-form-validator.pipe';

describe('DuplicateFormValidatorPipe', () => {
  it('should be defined', () => {
    expect(new DuplicateFormValidatorPipe()).toBeDefined();
  });
});
