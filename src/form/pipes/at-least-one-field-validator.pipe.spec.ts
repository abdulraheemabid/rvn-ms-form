import { AtLeastOneFieldValidatorPipe } from './at-least-one-field-validator.pipe';

describe('AtLeastOneFieldValidatorPipe', () => {
  it('should be defined', () => {
    expect(new AtLeastOneFieldValidatorPipe()).toBeDefined();
  });
});
