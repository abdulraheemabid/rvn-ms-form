import { ParentValidatorPipe } from './parent-validator.pipe';

describe('ParentValidatorPipe', () => {
  it('should be defined', () => {
    expect(new ParentValidatorPipe()).toBeDefined();
  });
});
