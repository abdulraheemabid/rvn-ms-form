export interface IRelationRepository {
    // findDefinitions(options: any): Promise<DefinitionEntity[]>;
    // findOneDefinition(id: number, options: any): Promise<DefinitionEntity>;
    // softDeleteDefinition(id: number): Promise<number>
    // saveDefinition(definition: DefinitionEntity, request: Request): Promise<DefinitionEntity>;
    // updateDefinition(definition: DefinitionEntity, request: Request): Promise<DefinitionEntity>;
}