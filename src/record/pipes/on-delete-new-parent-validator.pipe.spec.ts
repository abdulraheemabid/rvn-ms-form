import { OnDeleteNewParentValidatorPipe } from './on-delete-new-parent-validator.pipe';

describe('OnDeleteNewParentValidatorPipe', () => {
  it('should be defined', () => {
    expect(new OnDeleteNewParentValidatorPipe()).toBeDefined();
  });
});
